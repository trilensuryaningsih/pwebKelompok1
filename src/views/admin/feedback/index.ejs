<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= pageTitle %></title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body { font-family: 'Poppins', sans-serif; }
  </style>
</head>
<body class="bg-gray-100 font-sans">
  <div class="flex">
    <%- include('../../partials/admin/sidebar') %>

    <div class="flex-1 flex flex-col ml-64">
      <!-- Main Content -->
      <main class="flex-1 p-6">
        <h2 class="text-3xl font-bold text-gray-800 mb-6"><%= pageTitle %></h2>
        
        <!-- Tabel Feedback -->
        <div class="bg-white rounded-lg shadow-md overflow-x-auto">
          <table class="min-w-full">
            <thead class="bg-gray-50">
              <tr>
                <th class="py-3 px-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tanggal</th>
                <th class="py-3 px-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Pengguna</th>
                <th class="py-3 px-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No. HP</th>
                <th class="py-3 px-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Komentar</th>
                <th class="py-3 px-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                <th class="py-3 px-4 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Aksi</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200">
              <% feedbacks.forEach(function(feedback) { %>
                <tr class="hover:bg-gray-50">
                  <td class="py-4 px-4 whitespace-nowrap text-sm text-gray-900"><%= new Date(feedback.createdAt).toLocaleString('id-ID') %></td>
                  <td class="py-4 px-4 text-sm text-gray-900"><%= feedback.User.name %> (<%= feedback.User.email %>)</td>
                  <td class="py-4 px-4 text-sm text-gray-900"><%= feedback.phone_number || '-' %></td>
                  <td class="py-4 px-4 text-sm text-gray-600"><%= feedback.comment %></td>
                  <td class="py-4 px-4">
                    <span class="px-2 py-1 inline-flex text-xs leading-5 font-semibold rounded-full 
                      <%= feedback.status === 'approved' ? 'bg-green-100 text-green-800' : '' %>
                      <%= feedback.status === 'rejected' ? 'bg-red-100 text-red-800' : '' %>
                      <%= feedback.status === 'pending' ? 'bg-yellow-100 text-yellow-800' : '' %>">
                      <%= feedback.status %>
                    </span>
                  </td>
                  <td class="py-4 px-4 whitespace-nowrap">
                    <form action="/admin/feedback/<%= feedback.id %>/status" method="POST" class="inline-block">
                      <button type="submit" name="status" value="approved" class="text-sm text-green-600 hover:text-green-900">Approve</button>
                    </form>
                    <form action="/admin/feedback/<%= feedback.id %>/status" method="POST" class="inline-block ml-2">
                      <button type="submit" name="status" value="rejected" class="text-sm text-red-600 hover:text-red-900">Reject</button>
                    </form>
                    <form action="/admin/feedback/<%= feedback.id %>/delete" method="POST" class="inline-block ml-2">
                      <button type="submit" class="text-sm text-gray-500 hover:text-gray-900" onclick="return confirm('Apakah Anda yakin ingin menghapus feedback ini?')">Delete</button>
                    </form>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
          <% if (feedbacks.length === 0) { %>
            <p class="text-center py-4 text-gray-500">Belum ada feedback dari pengguna.</p>
          <% } %>
        </div>
      </main>
    </div>
  </div>
</body>
</html> 