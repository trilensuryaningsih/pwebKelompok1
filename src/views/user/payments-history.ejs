<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>UMCental - Riwayat Pembayaran</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet" />
    <style>
        .bg-custom-dark { background-color: #181C14; }
        .bg-custom-green { background-color: #697565; }
        .text-custom-green { color: #697565; }
        .border-custom-green { border-color: #697565; }
        .bg-custom-light-beige { background-color: #f5f0e9; }
        .text-custom-brown { color: #7f7a75; }
        .hover\:bg-custom-green-dark:hover { background-color: #5a6356; }
    </style>
</head>
<body class="flex min-h-screen bg-custom-light-beige">
    <%- include('../partials/sidebar') %>
    <div class="flex-1 flex flex-col">
        <%- include('../partials/header') %>
        <main class="flex-1 p-6 md:p-10">
            <!-- Header -->
            <div class="mb-8">
                <h1 class="text-3xl font-bold text-custom-dark mb-2">Riwayat Pembayaran</h1>
                <p class="text-custom-brown">Lihat semua catatan pembayaran dan denda Anda di sini.</p>
            </div>

            <!-- Statistics Cards -->
            <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
                <div class="bg-white rounded-xl shadow-md p-6 border border-gray-200">
                    <div class="flex items-center">
                        <div class="p-3 rounded-full bg-green-100 text-green-600">
                            <i class="fas fa-credit-card text-xl"></i>
                        </div>
                        <div class="ml-4">
                            <p class="text-sm font-medium text-gray-600">Total Pembayaran</p>
                            <p class="text-2xl font-semibold text-gray-900"><%= stats.totalPayments %></p>
                        </div>
                    </div>
                </div>
                <div class="bg-white rounded-xl shadow-md p-6 border border-gray-200">
                    <div class="flex items-center">
                        <div class="p-3 rounded-full bg-red-100 text-red-600">
                            <i class="fas fa-exclamation-triangle text-xl"></i>
                        </div>
                        <div class="ml-4">
                            <p class="text-sm font-medium text-gray-600">Total Denda</p>
                            <p class="text-2xl font-semibold text-gray-900"><%= stats.totalFines %></p>
                        </div>
                    </div>
                </div>
                <div class="bg-white rounded-xl shadow-md p-6 border border-gray-200">
                    <div class="flex items-center">
                        <div class="p-3 rounded-full bg-blue-100 text-blue-600">
                            <i class="fas fa-money-bill-wave text-xl"></i>
                        </div>
                        <div class="ml-4">
                            <p class="text-sm font-medium text-gray-600">Total Pembayaran</p>
                            <p class="text-2xl font-semibold text-gray-900"><%= formatNumber(stats.totalAmount) %></p>
                        </div>
                    </div>
                </div>
                <div class="bg-white rounded-xl shadow-md p-6 border border-gray-200">
                    <div class="flex items-center">
                        <div class="p-3 rounded-full bg-orange-100 text-orange-600">
                            <i class="fas fa-balance-scale text-xl"></i>
                        </div>
                        <div class="ml-4">
                            <p class="text-sm font-medium text-gray-600">Total Denda</p>
                            <p class="text-2xl font-semibold text-gray-900"><%= formatNumber(stats.totalFinesAmount) %></p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Error Message -->
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="mb-6 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
                    <i class="fas fa-exclamation-circle mr-2"></i>
                    <%= error %>
                </div>
            <% } %>

            <!-- Table Container -->
            <div class="bg-white rounded-xl shadow-md border border-gray-200 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="min-w-full">
                        <thead>
                            <tr class="bg-gray-50">
                                <th class="py-3 px-6 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tanggal</th>
                                <th class="py-3 px-6 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tipe</th>
                                <th class="py-3 px-6 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Metode</th>
                                <th class="py-3 px-6 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Jumlah</th>
                                <th class="py-3 px-6 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Detail Pesanan</th>
                                <th class="py-3 px-6 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                                <th class="py-3 px-6 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Bukti</th>
                                <th class="py-3 px-6 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Aksi</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-200">
                            <% if (paymentHistory && paymentHistory.length > 0) { %>
                                <% paymentHistory.forEach(item => { %>
                                    <tr class="hover:bg-gray-50 transition-colors">
                                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                            <%= formatDate(item.type === 'payment' ? item.paymentDate : item.createdAt) %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <% if (item.type === 'fine') { %>
                                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-red-100 text-red-800">
                                                    <%= item.displayType %>
                                                </span>
                                            <% } else { %>
                                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                                                    <%= item.displayType %>
                                                </span>
                                            <% } %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                            <%= item.paymentMethod || '-' %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-800">
                                            <%= formatNumber(item.amount) %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-sm">
                                            <a href="/user/orders/<%= item.Order.id %>" class="text-custom-green hover:underline">
                                                #<%= item.Order.id %>
                                            </a>
                                            <% if (item.itemDetails) { %>
                                                <br>
                                                <span class="text-xs text-gray-500">
                                                    <%= item.itemDetails.name %>
                                                </span>
                                            <% } %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full <%= getStatusClass(item.status) %>">
                                                <%= getStatusText(item.status) %>
                                            </span>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-sm">
                                            <% if (item.paymentProof && item.paymentProof !== '-') { %>
                                                <a href="/uploads/<%= item.paymentProof %>" target="_blank" class="text-custom-green hover:underline">
                                                    Lihat Bukti
                                                </a>
                                            <% } else { %>
                                                -
                                            <% } %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-sm">
                                            <a href="/user/payments/<%= item.type %>/<%= item.id %>" class="text-custom-green hover:underline">
                                                Detail
                                            </a>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="8" class="text-center py-16 px-6">
                                        <div class="mx-auto h-16 w-16 text-gray-400">
                                            <i class="fas fa-file-invoice-dollar fa-3x"></i>
                                        </div>
                                        <h3 class="mt-4 text-lg font-medium text-custom-dark">Tidak ada riwayat</h3>
                                        <p class="mt-1 text-sm text-custom-brown">Saat Anda melakukan pembayaran, catatannya akan muncul di sini.</p>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
        <%- include('../partials/footer') %>
    </div>
</body>
</html> 